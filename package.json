{
  "name": "babel-plugin-log-require",
  "scripts": {
    "prepublish": "npm run build",
    "build": "babel src --out-dir=dist",
    "test": "npm run build && mocha"
  },
  "license": "MIT",
  "main": "dist/babel-plugin-log-require.js",
  "description": "Babel plugin that inserts `console.log` messages at the top and bottom of each module in order to track runtime import behaviour.",
  "version": "1.0.2",
  "repository": {
    "type": "git",
    "url": "git@github.com:haggholm/babel-plugin-log-require.git"
  },
  "author": "Petter Häggholm <petter@petterhaggholm.net>",
  "devDependencies": {
    "@babel/cli": "^7.0.0-rc.1",
    "@babel/core": "^7.0.0-rc.1",
    "@babel/preset-env": "^7.0.0-rc.1",
    "chai": "^4.1.2",
    "mocha": "^5.2.0"
  },
  "peerDependencies": {
    "@babel/core": "^7.0.0-beta.47"
  }
}
